public class Ternary_Operators {
    public static void main() {
        int n = 4;
        int result = 0;

        result = n % 2 == 0 ? 10 : 20;

        System.out.println(result);
    }
}

// TERNARY = shorter form of if else
// TERNARY OPERATOR = ?:
